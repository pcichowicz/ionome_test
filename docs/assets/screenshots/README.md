# Screenshots

Used in `documentation.html`, in the "Using the Web App" tabbed section.
Drop the following files here (PNG, ~1200-1600px wide is plenty):

- `dashboard.png` — the main studies list, ideally with at least one row
  mid-run so the live status badges show up
- `new_study_wizard.png` — the "+ New Study" flow with sample metadata filled in
- `study_detail.png` — a study detail page showing QC / sample-status breakdown
- `sample_detail.png` — a sample detail page with one of the generated plots visible

This folder (and its contents) can be deleted once the real screenshots
replace these placeholders — it's just here so the image links in
documentation.html don't 404 before you've captured them.
